using UnityEngine;

public class CarInputSmoothing : MonoBehaviour
{
    /*[SerializeField] private Car _car;
    private const float _steeringSpeed = 2;
    private const float _steeringBackSpeed = 4;

    private ICarInputable _carInput;

    private void Start()
    {
        _carInput = _car.AIInput.IsAI ? _car.Hub.Input.CarInput : _car.AIInput;
    }

    public float Steer => _carInput.Steer;

    public float Force => _carInput.Force;

    public float Brake => _carInput.Brake;

    public float Handbrake => _carInput.Handbrake;*/

    
}
